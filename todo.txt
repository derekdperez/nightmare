Todo:

- Remove obsolete navigation links - Crawl Progress, Extractor Matches, Fuzzing, Database, and Docker Status. Any functionality present on any of the screens being removed that has not yet been mvoed to another screen should be now. Docker Status features go on the dashboard in the docker area, the rest should probably go in the Recon Results page.

- The dashboard should also contain performance stats - when it loads, have it display the data that was retrieved from the cache, and how long it took. Also display the data that was retrieved from the database, and how long that took as well. The dashboard should subscribe to all system events, and the System Events table should be updated in realtime as events are generated - it shouldn't poll or wait for periodic refreshes anymore.

- On the workflow builder, whem a plugin is selected from the plugin dropdown for a step, the Config JSON and Preconditions JSON should both be automatically populated - the config json should be configured to contain all required variables with default values, and all optional values shown without any configured values. this should change as the user changes the plugins. The preconditions should have several buttons after the Preconditions JSON label - "No Preconditions", which automatically loads json for no preonditions, "Previous Step Completion", which configures the preconditions to be the prior step completing successfully, "Wait for File" which populates the JSON with the preconditions necessary for the user to just enter a file path, and the task will run when the file exists. 

