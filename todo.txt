- The Recon Control and Recon Results pages are both appearing twice in the navigation buttons. They should only appear once each.

- Remove the following pages: Operations, Plugins, Workflw Runs, Fuzzing, Events, Auth0r, and Errors

- Move the Crawl Progress, Discovered Targets, Discovered Files, and Extractor Matches functionality to the Recon Results workflow output page, and then remove those pages as well.  

- The remaining pages should be: Workers, Workflows, any workflow-generated pages, Workflow Builder, Database, Docker Status, and View Logs.

- Generating workflow tasks on the Recon Control page says it is generating tasks, but they do not appear on the Workflow Monitor page.

- Remove the Enqueue Plugin Task and Reset Plugin Task sections from the Workflows page.

- Remove the Monitor and Timeline buttons from the Workflow page.

- In the Plugin Tasks section on the Workflows page, add the following to the Actions column: "Delete" - this deletes the task entirely. "Pause" - this sets the task to Paused, so no workers will pick it up for processing. "Run" - this causes a worker to pick the task up and begin processing, even if all prerequisites are not met.