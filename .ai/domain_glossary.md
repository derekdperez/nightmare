# Domain Glossary

- `workflow`: ordered plugin stage configuration persisted in `workflows/*.workflow.json`.
- `stage task`: coordinator row in `coordinator_stage_tasks` representing one domain+plugin execution unit.
- `workflow interface`: config-declared UI page (control/results) discovered from workflow JSON.
- `recon summary`: per-domain aggregate metrics exposed by `/api/coord/workflow-interface/recon/results`.
